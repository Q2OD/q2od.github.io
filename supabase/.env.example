# Supabase Edge Functions Environment Variables
# These are ONLY used in Edge Functions (server-side)
# NEVER commit actual .env file to git!

# R2 Credentials (from Cloudflare)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=caleb-media

# To set these in Supabase, run:
# supabase secrets set R2_ACCOUNT_ID=
# supabase secrets set R2_ACCESS_KEY_ID=9
# supabase secrets set R2_SECRET_ACCESS_KEY=a
# supabase secrets set R2_BUCKET_NAME=
